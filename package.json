{
  "name": "tylers-webdev-portfolio",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://tylerhance.github.io/tylers-webdev-portfolio/",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "next build && next export && git add out/ && git commit -m \"Deploy gh-pages\" && git subtree push --prefix out origin gh-pages"
    
  },
  "dependencies": {
    "next": "10.2.3",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "styled-components": "^5.3.0",
    "styled-normalize": "^8.0.7",
    "react-icons": "^4.2.0"
  }
}
